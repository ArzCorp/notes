import React from 'react'
import styled from 'styled-components'

const P = styled.p`
	color: pink;
	font-size: 20px;
`

export default function Logo() {
	return (
		<div>
			<P>Logo</P>
		</div>
	)
}
